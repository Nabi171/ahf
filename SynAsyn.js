// const colors = require("colors");

console.log('test the procedure');

setTimeout(() => {
    const user = { id: 1 };
    setTimeout(() => {
        const products = [{}, {}, {}];
        setTimeout(() => {
            const prices = [];
        }, 1000);
    }, 500)



}, 1000);

console.log('okay start');